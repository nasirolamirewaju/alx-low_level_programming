#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main-This is Entry prog for last
 * Return: it returns last digits 0
 */
int main(void)
{
	char c;

	char d;

	c = 'a';
	d = 'A';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	while
		(d <= 'z') {
			putchar (d);
			d++;
		}
	return (0);
}
