#include <stdio.h>
/**
 * main - this is res for entry point of func
 * Return: it returns 0
 */

int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", a);
	putchar('\n');
	return (0);
}
